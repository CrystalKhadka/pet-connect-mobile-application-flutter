import 'package:flutter/material.dart';

class ThemeConstant {
  // Dark theme colors
  static const Color darkPrimaryColor = Color(0xFF1EE532);
  static const Color darkSecondaryColor = Color(0xFF64F6A3);
  static const Color darkBackgroundColor = Color(0xFF121212);
  static const Color darkSurfaceColor = Color(0xFF1E1E1E);
  static const Color darkAppBarColor = Color(0xFF212121);

  // Light theme colors
  static const Color lightPrimaryColor = Color(0xFF6EF321);
  static const Color lightSecondaryColor = Color(0xFF64F697);
  static const Color lightBackgroundColor = Color(0xFFF5F5F5);
  static const Color lightSurfaceColor = Colors.white;
  static const Color lightAppBarColor = Color(0xFF21F332);

  // Common colors
  static const Color errorColor = Colors.red;
  static const Color successColor = Colors.green;
  static const Color warningColor = Colors.orange;
  static const Color infoColor = Colors.blue;

  // Text colors
  static const Color darkTextColor = Colors.white;
  static const Color lightTextColor = Colors.black;

  // Icon colors
  static const Color darkIconColor = Colors.white70;
  static const Color lightIconColor = Colors.black54;

  // Border colors
  static const Color darkBorderColor = Colors.white24;
  static const Color lightBorderColor = Colors.black12;

  // Opacity values
  static const double highEmphasisOpacity = 0.87;
  static const double mediumEmphasisOpacity = 0.60;
  static const double disabledOpacity = 0.38;

  // Font sizes
  static const double headlineFontSize = 24.0;
  static const double titleFontSize = 20.0;
  static const double bodyFontSize = 16.0;
  static const double captionFontSize = 12.0;

  // Spacing
  static const double smallSpacing = 8.0;
  static const double mediumSpacing = 16.0;
  static const double largeSpacing = 24.0;

  // Border radius
  static const double smallBorderRadius = 4.0;
  static const double mediumBorderRadius = 8.0;
  static const double largeBorderRadius = 16.0;

  // Elevation
  static const double lowElevation = 2.0;
  static const double mediumElevation = 4.0;
  static const double highElevation = 8.0;
}
